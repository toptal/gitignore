# poetry
#   Similar to Pipfile.lock, it's generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
poetry.lock

# The requirements must be recreated using `poetry export`
requirements.txt

# The output from `poetry build` can't be included in the repository
/dist
